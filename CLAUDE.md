# CLAUDE.md

Project guidance for Claude Code is maintained in `AGENTS.md` (implementation
reference, directory layout, build dependencies). It is imported below so this
file stays the single source of truth and never drifts.

@AGENTS.md
