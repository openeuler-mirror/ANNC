{
  "version": 1,
  "patterns": [
    {
      "name": "topk_multi_output",
      "kernel": "fused_topk_multi_output",
      "graph": [
        "(values, indices) = TopK(input, k)"
      ],
      "inputs": ["input", "k"],
      "outputs": ["values", "indices"]
    }
  ]
}
